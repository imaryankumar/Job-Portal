export interface cardTypes {
    location?: string;
    title?: string;
    description?: string;
    id?: string;
    updatedAt?: any;
  }

  export interface jobData {
    email: string;
    name: string;
    skills: string;
    id: string;
  }